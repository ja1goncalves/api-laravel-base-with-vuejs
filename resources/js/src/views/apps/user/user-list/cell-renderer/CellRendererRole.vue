<template>
    <vs-chip class="ag-grid-cell-chip" :color="chipColor(params.value)">
      <span>{{ this.getName(params.value) }}</span>
    </vs-chip>
</template>

<script>
export default {
  name: 'CellRendererRole',
  methods: {
    getName(value) {
      if (value === 'admin') return 'ADM'
      else if (value === 'editor') return 'Editor'
      else if (value === 'public') return 'Público'
      else return '--'
    }
  },
  computed: {
    chipColor () {
      return (value) => {
        if (value === 'admin') return 'primary'
        else if (value === 'editor') return 'gray'
        else if (value === 'public') return 'light'
        else return 'danger'
      }
    }
  }
}
</script>

<style lang="scss" scpoped>
.ag-grid-cell-chip {
  &.vs-chip-success {
    background: rgba(var(--vs-success),.15);
    color: rgba(var(--vs-success),1) !important;
    font-weight: 500;
  }
  &.vs-chip-warning {
    background: rgba(var(--vs-warning),.15);
    color: rgba(var(--vs-warning),1) !important;
    font-weight: 500;
  }
  &.vs-chip-danger {
    background: rgba(var(--vs-danger),.15);
    color: rgba(var(--vs-danger),1) !important;
    font-weight: 500;
  }
}
</style>
